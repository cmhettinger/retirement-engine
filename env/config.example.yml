supported_workbook_version: "0.1.0"

# Default workbook to use when none is supplied on the CLI.
default_workbook: "resources/workbooks/example.xlsx"

# Default output directory.
output_directory: "build"

reports:
  default_format: "pdf"
  include_charts: true
