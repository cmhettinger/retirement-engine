# Default workbook to use when none is supplied on the CLI.
default_workbook: ~/Documents/Retirement/my-retirement.xlsx

# Default output directory.
output_directory: build

# Workbook schema expected by this version of the engine.
required_workbook_version: 0.1.0

reports:
  default_format: pdf
  include_charts: true
